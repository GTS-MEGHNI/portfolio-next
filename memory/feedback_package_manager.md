---
name: Use bun as package manager
description: This project uses bun, not npm or yarn
type: feedback
---

Always use `bun` as the package manager for this portfolio project, not `npm`, `yarn`, or `pnpm`.

**Why:** The user corrected this explicitly — the project uses bun (bun.lock is present).

**How to apply:** Use `bun` for all install, run, and script commands (e.g., `bun dev`, `bun add`, `bun install`).
